name: eraffle
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  sqflite: ^2.0.0+3
  flutter_svg: ^0.22.0
  path_provider: ^2.0.2
  webview_flutter: ^2.0.9
  dropdown_below: ^0.0.8
  flutter_fortune_wheel: ^1.0.1
  group_button: ^2.3.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/Icons/
    - assets/db/


